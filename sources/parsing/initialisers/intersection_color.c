#include "minirt.h"

/*
//
int	intersection_color_sphere(t_obj *object, t_hit hit)
{
	int		f1;
	int		f2;
	t_vec	vec;

	color = 0;
	vec = vec_sub(hit->point, obj->pos);
	vec_normalize(vec);
	f2 = roundf(8 * acosf(vec.y) / M_PI);
	f3 = roundf(8 * atan2f(vec.z, vec.x) / M_PI);
	if ((f2 + f3) % 2)
		return (obj->col1);
	return (obj->col2);


}

//
int	intersection_color_plane()
{

}

//
int	intersection_color_cylinder()
{

}

*/
